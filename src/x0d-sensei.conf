# x0 (xzero) HTTP web server: configuration file (sensei.trapni.de)
# vim:syntax=flow

import 'dirlisting'
import 'accesslog'
#import 'compress'
import 'fastcgi'
import 'userdir'
import 'vhost'
#import 'proxy'
import 'cgi'
import 'rrd'
#import 'ssl'

var ialu_rails = '127.0.0.1:2001'  # spawn-fcgi -u www -p 2001 -F 32 -- /opt/ialu/public/dispatch.fcgi
var ialu_chat = '127.0.0.1:2002'   # (systemd) /opt/ialu/bin/ialu_chatd
var redmine_srv = '127.0.0.1:2003' # spawn-fcgi -u www -p 2003 -F 8 -- /var/www/redmine.xzero.ws/redmine/public/dispatch.fcgi
var php_srv = '127.0.0.1:2004'     # spawn-fcgi -u www -p 2004 -F 8 -C 8 -- /usr/bin/php-cgi

# rules to be invoked at service startup
handler setup
{
	log.level 10

	mimetypes '/etc/mime.types'
	mimetypes.default 'text/plain'

	userdir.name "public_html";

	# dynamic content compression
/*	compress.level 9
	compress.min 4 byte
	compress.max 128 mbyte
	compress.types [
		"text/plain",
		"text/html",
		"text/css",
		"application/xml",
		"application/xhtml+xml"
	] */

	# HTTPS (SSL)
#	ssl.listen '85.214.83.172:443'
#	ssl.loglevel 0
#	ssl.context 'certfile' => '/home/trapni/projects/ialu/ssl/ialu.de.crt',
#				'keyfile' => '/home/trapni/projects/ialu/ssl/ialu.de.key'

	# HTTP (plain)
	listen '85.214.83.172:80'

	# RRD
	rrd.filename "/home/trapni/projects/x0/www/htdocs/rrd/x0d.rrd"
	rrd.step 60

	# CGI
	cgi.mapping '.php' => '/usr/lib64/php5.2/bin/php-cgi'

	#vhost.add 'ninchens.net' => '/var/www/ninchens.net/htdocs'
	vhost.mapping 'www.xzero.ws:80' => { redirect 'http://xzero.ws' },
				  'xzero.ws' => xzero,
				  'www.ialu.de:80' => { redirect 'http://ialu.de' },
				  'www.ialu.de:443' => { redirect 'https://ialu.de' },
				  'ialu.de' => ialu,
				  'redmine.xzero.ws' => { redirect 'http://redmine.trapni.de' },
				  'redmine.trapni.de' => redmine,
				  'www.irony-of-fate.de' => iof,
				  'irony-of-fate.de' => { redirect 'http://www.irony-of-fate.de' },
				  'narutoplay.ninchens.net' => narutoplay,
				  'zelda-rpg.ninchens.net' => zeldarpg,
				  'mail.trapni.de' => mail,
				  'blog.trapni.de' => blog,
				  'blog.xzero.ws' => blog, #{ redirect 'http://blog.trapni.de' },
				  'rena.ninchens.net' => rena,
				  'caelum.ninchens.net' => caelum,
				  'www.ninchens.net' => { redirect 'http://ninchens.net' },
				  'www.ninchens.de' => { redirect 'http://ninchens.net' },
				  'ninchens.de' => { redirect 'http://ninchens.net' },
				  'ninchens.net' => ninchens,
				  'www.trapni.de' => { redirect 'http://trapni.de' },
				  'trapni.de' => trapni.de
}

# every requests' entry point is main
handler main {
	rrd
	accesslog '/var/log/apache2/x0d_access.log'
	vhost.map
	#if req.path == '/favicon.ico' respond 404
}

# {{{ helper handlers
# processes PHP files via php-cgi, connected via unix domain socket through the fastcgi protocol:
handler php {
	pathinfo

	if phys.path =$ '.php' {
		cgi.map
		#fastcgi php_srv
	}
}

handler default {
	autoindex 'index.php', 'index.html'
	php
	staticfile
	dirlisting
}
# }}}

# {{{ website handlers
handler xzero {
	docroot '/var/www/xzero.ws/htdocs'
	userdir
	autoindex 'index.html'

	if req.path =^ '/public' then dirlisting

	pathinfo
	cgi.exec if phys.path =$ '.cgi'

	staticfile
	dirlisting
}

handler ialu {
	docroot '/home/trapni/projects/ialu/public'
	staticfile

	if req.path =~ /^\/chat\/(listen|send)$/ {
		# chat app
		fastcgi ialu_chat
	} else {
		# rails app
		fastcgi ialu_rails
	}
}

handler redmine {
	docroot '/var/www/redmine.xzero.ws/redmine/public'
	staticfile
	fastcgi redmine_srv
}

handler iof {
	docroot '/var/www/irony-of-fate.de/htdocs'
	userdir
	default
}

handler narutoplay {
	docroot '/var/www/narutoplay.ninchens.de/htdocs'
	default
}

handler zeldarpg {
	docroot '/var/www/zelda-rpg.ninchens.net/htdocs'
	default
}

handler mail {
	docroot '/var/www/mail.trapni.de/htdocs'
	default
}

handler blog {
	docroot '/var/www/blog.xzero.ws/htdocs'
	default
}

handler rena {
	docroot '/home/liza/html_rena'
	default
}

handler caelum {
	docroot '/home/liza/html_caelum'
	default
}

handler ninchens {
	#accesslog '/var/www/ninchens.net/logs/access.log'

	if phys.path =$ '.csp' or req.path =^ '/chat/listen'
	{
		fastcgi '127.0.0.1:2002'
	}
	else
	{
		staticfile
	}
	# unhandled path
	respond 404
}

handler trapni.de {
	docroot '/var/www/trapni.de/htdocs'
	userdir
	default
}
# }}}
