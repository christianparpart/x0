
Install Requirements:

- GCC 4.4.1 - (it really must be 4.4.0 or higher)
- LUA 5.1.3
- boost 1.39
- cmake - for building
- cppunit
- Asio - standalone version (ideally: linux-perf-branch)

fetch the sources from github.com/trapni/x0 (i guess you already did that).

# cmake .
# make edit_cache
# make
# sudo make install
